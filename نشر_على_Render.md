# نشر نظام ركاز على Render — للعميل

بعد هذه الخطوات يحصل العميل على رابط دائم يفتح من أي جهاز دون تشغيل جهازك.

## المتطلبات

- حساب على [GitHub](https://github.com)
- حساب على [Render](https://render.com)
- المشروع مرفوع على GitHub: https://github.com/wadnooh/rekaz

---

## 1) المستودع جاهز

الكود موجود على:

https://github.com/wadnooh/rekaz

إذا عدّلت محلياً ارفع التحديثات:

```bash
git add .
git commit -m "Update Rekaz app"
git push origin main
```

---

## 2) إنشاء الخدمة على Render

### الأسرع (زر واحد — للمراجعة)

افتح:

https://render.com/deploy?repo=https://github.com/wadnooh/rekaz

أو شغّل الملف: `نشر_سحابي_للعميل.bat`

ثم:
1. اربط حساب GitHub إن طُلب
2. اختر الخطة **Free** للمراجعة
3. اضغط **Deploy**
4. انتظر حتى تصبح Live وانسخ الرابط للعميل

الخدمة تُنشأ باسم `rekaz-alenjaz` مع Waitress و`SECRET_KEY` تلقائي.

> للمراجعة المجانية البيانات قد تُعاد عند إعادة النشر. للاستخدام الإنتاجي لاحقاً يمكن ترقية الخطة وإضافة Disk.

### الطريقة اليدوية

1. **New** → **Web Service**
2. اختر المستودع
3. الإعدادات:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python -m webapp.app`
   - **Instance:** Starter (مع Disk)
4. Environment Variables:

| المفتاح | القيمة |
|---------|--------|
| `HOST` | `0.0.0.0` |
| `USE_WAITRESS` | `1` |
| `RAKAZ_DATA_DIR` | `/var/data` |
| `SECRET_KEY` | قيمة عشوائية طويلة (أو Generate) |
| `PYTHON_VERSION` | `3.12.8` |

5. أضف **Disk**: المسار `/var/data` وحجم 1 GB
6. **Create Web Service**

---

## 3) الرابط للعميل

بعد نجاح النشر انسخ الرابط، مثال:

`https://rekaz-alenjaz.onrender.com`

أرسله للعميل — يفتح من الجوال أو أي متصفح.

### بيانات الدخول الافتراضية

- المستخدم: `admin`
- كلمة المرور: `admin123`

**مهم:** غيّر كلمة المرور فوراً من شاشة المستخدمين بعد أول دخول.

---

## 4) تشغيل تجريبي على الشبكة المحلية (بدون Render)

إذا أردت تجربة الوصول من أجهزة على نفس الـ Wi‑Fi فقط:

شغّل الملف: `تشغيل للشبكة.bat`

ثم افتح من الجهاز الآخر العنوان الذي يظهر في النافذة (مثل `http://192.168.x.x:5070`).

هذا الوضع **يتطلب** أن يبقى جهازك شغّالاً — بخلاف Render.

---

## 5) أعطال شائعة

| المشكلة | الحل |
|---------|------|
| الخدمة تنام ثم تبطئ (Free) | استخدم Starter كما في الدليل |
| البيانات اختفت بعد Redeploy | تأكد من Disk و`RAKAZ_DATA_DIR=/var/data` |
| خطأ في التشغيل | راجع Logs في Render وتأكد من `requirements.txt` |
| لا يفتح من الخارج محلياً | استخدم Render أو تأكد أن `HOST=0.0.0.0` وجدار الحماية يسمح بالمنفذ |

---

## ملخص للعميل

1. افتح الرابط الذي وصلك.
2. سجّل الدخول بالحساب المعطى لك.
3. غيّر كلمة المرور.
4. ابدأ استخدام النظام من أي جهاز.
