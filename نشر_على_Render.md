# نشر نظام ركاز على Render — مرحلة التجربة ثم المدفوع

## الوضع الحالي: تجربة على السيرفر المجاني

التطبيق يعمل الآن على **Render Free** للمراجعة والتجربة:

**https://rekaz-alenjaz.onrender.com**

لوحة الخدمة: https://dashboard.render.com/web/exs-d9jpvf7lk1mc738luveg  
المستودع: https://github.com/wadnooh/RKAZ

| البند | التجربة المجانية | بعد الاعتماد (مدفوع) |
|-------|------------------|----------------------|
| الخطة | `plan: free` + `TRIAL_MODE=1` | Starter/أعلى + Disk |
| ثبات البيانات | غير مضمون بعد Redeploy | `RAKAZ_DATA_DIR=/var/data` |
| الحفظ | إنشاء حفظة + **تنزيل ZIP للجهاز** | حفظ تلقائي على القرص الدائم |
| السكون | ينام مع الخمول (إقلاع بطيء أول مرة) | أقل سكوناً |

### ماذا تفعل أثناء التجربة؟

1. ادخل: `/admin/backups` (حفظ البيانات)
2. أنشئ حفظة بعنوان واضح (مثال: «تجربة أسبوع 1»)
3. اضغط **تنزيل** واحفظ ZIP على جهازك
4. إذا اختفت البيانات بعد نشر جديد: **رفع الحفظة** من نفس الشاشة مع تفعيل الاستعادة

---

## المتطلبات

- حساب على [GitHub](https://github.com)
- حساب على [Render](https://render.com)
- المستودع: https://github.com/wadnooh/RKAZ

---

## 1) رفع التحديثات

```bash
git remote add origin https://github.com/wadnooh/RKAZ.git
git add .
git commit -m "Update Rekaz"
git push -u origin main
```

---

## 2) التجربة المجانية (الوضع الحالي)

`render.yaml` مضبوط على:

- `plan: free`
- `TRIAL_MODE=1`
- بدون `RAKAZ_DATA_DIR` (لا قرص دائم على المجاني)
- `SECRET_KEY` عبر `sync: false` — يُدار من لوحة Render فقط ولا يُولَّد من جديد عند كل مزامنة

> **مهم:** لا تغيّر `SECRET_KEY` بعد ضبطه. تغييره يُبطل كوكيز الجلسة ويخرج جميع المستخدمين.

### ربط Render بالمستودع (مرة واحدة)

1. افتح: https://render.com/deploy?repo=https://github.com/wadnooh/RKAZ  
   أو شغّل: `ربط_Render.bat`
2. سجّل دخول Render بحساب GitHub (`wadnooh`)
3. وافق على المستودع `wadnooh/RKAZ` إن طُلب
4. اضغط **Apply / Deploy** على الخطة Free
5. انتظر حتى تصبح الحالة **Live**

> أول فتح بعد السكون قد يتأخر دقيقة — طبيعي على Free.

---

## 3) الترقية للمدفوع (عند الاعتماد الكامل)

1. في Render: غيّر Instance إلى **Starter** (أو أعلى)
2. أضف **Disk**: المسار `/var/data` — حجم 1 GB
3. Environment Variables:

| المفتاح | القيمة |
|---------|--------|
| `TRIAL_MODE` | `0` |
| `RAKAZ_DATA_DIR` | `/var/data` |
| `HOST` | `0.0.0.0` |
| `USE_WAITRESS` | `1` |
| `SECRET_KEY` | **لا تغيّره** — ثبّته مرة واحدة في Environment؛ تغييره يُسجّل خروج جميع المستخدمين |

4. ارفع آخر حفظة ZIP من التجربة عبر `/admin/backups` → رفع + استعادة
5. احذف شريط «وضع التجربة» تلقائياً بعد `TRIAL_MODE=0`

---

## 4) الرابط للعميل أثناء التجربة

`https://rekaz-alenjaz.onrender.com`

- المستخدم: `admin`
- كلمة المرور: `admin123` (غيّرها بعد أول دخول)

---

## 5) أعطال شائعة

| المشكلة | الحل |
|---------|------|
| الخدمة تنام ثم تبطئ (Free) | انتظر دقيقة أو حدّث الصفحة — طبيعي بالتجربة |
| البيانات اختفت بعد Redeploy | ارفع ZIP من جهازك عبر حفظ البيانات |
| خروج المستخدمين بعد التحديث | تأكد أن `SECRET_KEY` موجود وثابت في Environment — لا تدورّه ولا تحذفه |
| تريد ثبات دائم | رقِّ للمدفوع + Disk كما فوق |
| خطأ تشغيل | راجع Logs في Render |

---

## ملخص

1. حالياً: **تجربة مجانية** + حفظات تُنزَّل للجهاز.
2. لاحقاً: **مدفوع + Disk** = حفظ دائم على السيرفر.
3. مسار الحفظ داخل التطبيق: المستخدمون/الإعدادات → **حفظ البيانات**.
