# متقاعد — كان يُبقي خدمة Render المجانية مستيقظة.
# الاستضافة الرسمية الآن: https://rekaz.wadnooh.com (VPS دائم، لا يحتاج keep-alive).
# عُطّل الـ schedule حتى لا يستدعي الرابط القديم onrender.

name: Keep Render Awake (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: |
          echo "Render keep-alive retired. Official URL: https://rekaz.wadnooh.com"
          exit 0
